import React from 'react';
import ReactDOM from 'react-dom';

class Header extends React.Component {
  render(){
    return (
      // <h1 className="header">{this.props.title}</h1>
      <h2 className="header"><b>sat</b>elite</h2>
    );
  }
}

export default Header;
